name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: "Hey welcome, congratulations on creating your first issue in our repository🥳 "
        pr-message: "Hey welcome, congratulations on creating your first pull request in our repository🥳"
